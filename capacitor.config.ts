import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.viswashbandhan',
  appName: 'viswash-bandhan',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
